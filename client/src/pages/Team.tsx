import { Container } from "@nextui-org/react";
import { Teams } from "components/teams";

const Team: React.FC = () => {
  return (
    <Container>
      <Teams />
    </Container>
  );
};

export default Team;
