const home = "/";
const dashboard = "/dashboard";
const projects = "/projects";
const tasks = "/tasks";
const teams = "/teams";

export const pages = {
  dashboard,
  projects,
  tasks,
  teams,
  home,
};
