import { Text } from "@nextui-org/react";

const BrandLogo: React.FC = () => {
  return (
    <Text size={24} weight="bold" css={{ color: "$primary" }}>
      Taskify
    </Text>
  );
};

export default BrandLogo;
